services:
  - type: web
    name: instagram-scraper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: APIFY_TOKEN
        sync: false
